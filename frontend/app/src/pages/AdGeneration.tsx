import { PlaceholderPage } from './PlaceholderPage';

export function AdGenerationPage() {
  return (
    <PlaceholderPage
      eyebrow="Step 4 / 4"
      title="Generate Ads"
      description="Turn approved brand, asset, and detection data into ad variants. Pick templates, ratios, and platforms; preview, edit, and export."
      cta="Ad Generation page rebuild — Phase 7"
      legacyHref="/ads.html"
    />
  );
}
