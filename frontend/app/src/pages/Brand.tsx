import { PlaceholderPage } from './PlaceholderPage';

export function BrandPage() {
  return (
    <PlaceholderPage
      eyebrow="Step 1 / 4"
      title="Brand Setup"
      description="Capture brand identity, sources, visual rules, and default ad preferences. Everything generated downstream pulls from this profile."
      cta="Brand page rebuild — Phase 4"
      legacyHref="/brand.html"
    />
  );
}
