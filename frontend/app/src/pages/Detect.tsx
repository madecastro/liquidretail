import { PlaceholderPage } from './PlaceholderPage';

export function DetectPage() {
  return (
    <PlaceholderPage
      eyebrow="Step 3 / 4"
      title="AI Detection Review"
      description="Review what AI found inside uploaded assets — products, scenes, people, text, safe zones, match scores, rights and safety flags."
      cta="Detect page rebuild — Phase 6"
      legacyHref="/detect.html"
    />
  );
}
